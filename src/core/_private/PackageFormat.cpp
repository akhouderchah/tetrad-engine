#include "core/PackageFormat.h"

namespace tetrad {
const char PackageFormat::ID[3] = {0xc, 'p', 'k'};
const PackageFormat::projID_t PackageFormat::PROJ_ID = {'t', 'e', 't', 'r', 'd'};

}  // namespace tetrad
